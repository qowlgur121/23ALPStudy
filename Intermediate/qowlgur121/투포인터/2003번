#include <iostream>

int a[10005];
int st, en, sum, cnt;

int main() {
	int n,m;
	std::cin >> n >> m;

	for (int i = 0; i < n; i++) {
		std::cin >> a[i];
	}


	while (en <= n) {
		if (sum >= m)sum -= a[st++];
		if (sum < m)sum += a[en++];
		if (sum == m) cnt++;
	}

	std::cout << cnt;

}
